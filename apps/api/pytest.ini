[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    e2e: API upload + process integration tests (may be slower)
filterwarnings =
    ignore::DeprecationWarning
